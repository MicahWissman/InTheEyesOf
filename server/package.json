{
  "name": "ask-proxy",
  "version": "1.0.0",
  "description": "Thin Gemini proxy — keeps API key server-side, never in the client bundle",
  "main": "ask-proxy.js",
  "scripts": {
    "start": "node ask-proxy.js",
    "dev": "node --watch ask-proxy.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
